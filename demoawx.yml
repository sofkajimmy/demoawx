- name: copy file
  hosts: all
  tasks:
    - name: Copy file
      debug: 
        msg: "echo saludos"
    - name: Create a directory if it does not exist
      ansible.builtin.file:
        path: /etc/some_directory
        state: directory
        mode: '0755'